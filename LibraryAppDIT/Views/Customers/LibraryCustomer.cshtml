@model IEnumerable<LibraryAppDIT.Models.Customer>
@{
    ViewBag.Title = "LibraryCustomer";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section scripts
{
    <script>
        $(document).ready(function ()
        {
            var table = $("#customers").DataTable({
                ajax: {
                    url: "/api/customers",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "Name",
                        render: function (data, type, customer) {
                            return "<a href='CxDetails/" + customer.Id + "'>" + customer.Name + "</a>";
                        }
                    },
                    {
                        data: "Address"
                    },
                    {
                        data: "Id",
                        render: function(data){
                            return "<button class='btn-link js-delete' data-customer-id=" + data + ">Delete</button>";
                        }       
                    }
                ]
            });

            $("#customers").on("click", ".js-delete", function () {

                var deleteBtn = $(this);

                if(confirm("Are you sure you want to delete this customer?")){
                        $.ajax({
                            url: "/api/customers/" + deleteBtn.attr("data-customer-id"),
                            method: "DELETE",
                            success: function () {
                                table.row(deleteBtn.parents("tr")).remove().draw();
                            }
                        });
                    }
                });
            });
       // });

    </script>    
}

<h2>Customers</h2>
@*@if (!Model.Any())
{
    <p> We don't have any customers yet. </p>
}
else
{*@

<br />

@Html.ActionLink("Add Member", "MemberForm", "Customers", null, new { @class = "btn btn-primary" })

<br />  
<br />

<table id="customers" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Customer</th>
            <th>Address</th>
            <th>Delete </th>
        </tr>
    </thead>

    <tbody>
        @*@foreach (var customer in Model)
        {
            <tr>
                <td>@Html.ActionLink(customer.Name, "CxDetails", "Customers", new { id = customer.Id }, null) </td>
                <td>@customer.Address</td>
                <td>
                    <button data-customer-id="@customer.Id" class="btn-link js-delete">Delete</button>
                </td>
            </tr>
        }*@
    </tbody>
</table>


